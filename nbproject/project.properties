annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.processors.list=
annotation.processing.run.all.processors=true
application.title=Eq_Nexum_Proyecto
application.vendor=mdmcode
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.generated.dir}/sources
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
compile.on.save=false
dist.dir=dist
dist.jar=${dist.dir}/Eq_Nexum_Proyecto.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=
file.reference.src=src
includes=**
jar.compress=false
javac.classpath=
javac.compilerargs=
javac.deprecation=false
javac.processorpath=${javac.classpath}
javac.source=17
javac.target=17
javac.test.classpath=${javac.classpath}:${build.classes.dir}
main.class=controlador.Main
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
mkdist.disabled=false
platform.active=default_platform
run.classpath=${javac.classpath}:${build.classes.dir}
run.test.classpath=${javac.test.classpath}:${build.test.classes.dir}
src.dir=src
src.dir.display=Sources
